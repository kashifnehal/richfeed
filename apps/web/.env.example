# Supabase (anon key only — never the service role key here)
# Supabase dashboard → Project Settings → API
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# API
API_URL=http://localhost:4000
