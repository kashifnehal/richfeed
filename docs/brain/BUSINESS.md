# BUSINESS.md — placeholder stub

> **This is an auto-generated stub.** The real `BUSINESS.md` was authored in the
> planning thread and should be dropped into `docs/brain/` to replace this file
> wholesale. Do not build this out in place — replace it.

## What we know from the repo in the meantime

- **Owner / first customer:** Blue Beacon Research (BBR). RichFeed is currently
  an internal BBR tool; there are no external users or revenue yet.
- **Stage:** brand-new product in active build-out. Local-only, no deployment.
- **Two-track delivery model** (see `DECISIONS.md`): Track A handles external
  approval gates (platform API access, business verification, app review);
  Track B is engineering. They run in parallel and neither is allowed to block
  the other.
- **Platform-policy landscape** drives the roadmap: platforms are grouped into
  tiers by integration feasibility / review burden (see `platforms/STATUS.md`).
  LinkedIn personal-profile posting is first because it has no review gate.

See `platforms/STATUS.md` for the per-platform blocker picture.
